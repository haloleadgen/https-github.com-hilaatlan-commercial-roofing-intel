---
interface Props {
  items: string[];
}
const { items } = Astro.props;
---

<aside class="takeaways" aria-labelledby="key-takeaways">
  <h2 id="key-takeaways">Key takeaways</h2>
  <ul>
    {items.map((item) => <li>{item}</li>)}
  </ul>
</aside>
