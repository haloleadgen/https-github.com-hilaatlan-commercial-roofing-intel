---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE } from '../data/site';

interface Props {
  title: string;
  description?: string;
  /** Set false on utility pages (search, 404) to exclude from indexing */
  noindex?: boolean;
  /** Optional JSON-LD object rendered in addition to the Organization schema */
  schema?: Record<string, unknown>;
}

const { title, description = SITE.description, noindex = false, schema } = Astro.props;
const canonical = new URL(Astro.url.pathname, SITE.domain).href;
const fullTitle =
  Astro.url.pathname === '/' ? `${SITE.name} — ${SITE.descriptor}` : `${title} | ${SITE.name}`;

/**
 * Per-page Open Graph card. Indexable pages get a generated branded card
 * (built post-build by scripts/generate-og.mjs from this exact URL convention);
 * the homepage and noindexed pages keep the default brand card.
 */
const ogSlug = Astro.url.pathname.replace(/^\/|\/$/g, '').replace(/\//g, '-');
const ogImage =
  noindex || ogSlug === '' ? `${SITE.domain}/og-default.png` : `${SITE.domain}/og/${ogSlug}.png`;

const orgSchema = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: SITE.name,
  url: SITE.domain,
  description: SITE.description,
  email: SITE.contactEmail,
  telephone: '+1-954-634-2028',
  logo: `${SITE.domain}/favicon.svg`,
  publishingPrinciples: `${SITE.domain}/about/editorial-standards/`,
  correctionsPolicy: `${SITE.domain}/about/corrections/`,
  ownershipFundingInfo: `${SITE.domain}/about/funding-and-relationships/`,
  founder: {
    '@type': 'Person',
    name: 'Hila Atlan',
    jobTitle: 'Founder & Editor-in-Chief',
    url: `${SITE.domain}/about/#who-is-behind-cri`,
  },
};

const websiteSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: SITE.name,
  url: SITE.domain,
  potentialAction: {
    '@type': 'SearchAction',
    target: { '@type': 'EntryPoint', urlTemplate: `${SITE.domain}/search/?q={search_term_string}` },
    'query-input': 'required name=search_term_string',
  },
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    {noindex && <meta name="robots" content="noindex, follow" />}

    <meta name="theme-color" content="#0d2b45" />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="en_US" />
    <meta property="og:site_name" content={SITE.name} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={`${title} — Commercial Roofing Intel`} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={ogImage} />

    <script type="application/ld+json" set:html={JSON.stringify(orgSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
    {schema && <script type="application/ld+json" set:html={JSON.stringify(schema)} />}
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to main content</a>
    <Header />
    <main id="main-content">
      <slot />
    </main>
    <Footer />
  </body>
</html>
